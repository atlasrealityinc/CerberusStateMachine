namespace Cerberus.Tests.TestHelpers
{
    public enum TestStateId { State1, State2, State3 }
    public enum TestEventId { Event1, Event2, Event3 }
    public enum TestSubStateId { SubState1, SubState2, SubState3 }
    public enum TestSubEventId { SubEvent1, SubEvent2 }
    public enum TestMachineEventId { MachineEvent1, MachineEvent2 }
    public enum TestGrandchildStateId { GrandchildState1, GrandchildState2 }
    public enum TestGrandchildEventId { GrandchildEvent1 }
}
